name: pychunkedgraph
channels:
  - conda-forge
dependencies:
  - python==3.11.4
  - pip
  - tox
  - uwsgi==2.0.21
  - graph-tool-base==2.58
  - pip:
    - -r requirements.txt
    - -r requirements-dev.txt