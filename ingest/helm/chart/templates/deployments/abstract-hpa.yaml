{{- if .Values.abstractWorkers.hpa.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.abstractWorkers.name | quote }}
  labels:
    app: {{ .Values.abstractWorkers.name | quote }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.abstractWorkers.name | quote }}
  minReplicas: {{ .Values.abstractWorkers.hpa.minReplicas }}
  maxReplicas: {{ .Values.abstractWorkers.hpa.minReplicas }}
  metrics:
  {{- if .Values.abstractWorkers.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.abstractWorkers.hpa.targetCPU }}
  {{- end }}
  {{- if .Values.abstractWorkers.hpa.targetMem }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.abstractWorkers.hpa.targetMem }}
  {{- end }}
{{- end }}
