apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: pcg-hpa
  namespace: default
  labels:
    app: pcg-test  
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pcg-worker
  minReplicas: 1
  maxReplicas: 1000
  targetCPUUtilizationPercentage: 80